<Window x:Class="HyLordServerUtil.BanReasonDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ban Player"
        Width="420"
        Height="220"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#14161A">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" Text="Ban" Foreground="#E6E6E6" FontSize="18" FontWeight="Bold"/>

        <TextBlock Grid.Row="1" Text="Reason (optional)" Foreground="#AAA" Margin="0,12,0,6"/>

        <TextBox x:Name="ReasonBox" Grid.Row="2" Background="#0E1013" Foreground="#FFFFFF" BorderBrush="#333" AcceptsReturn="False" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Width="90" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Ban" Width="90" Click="Ban_Click"/>
        </StackPanel>
    </Grid>
</Window>
